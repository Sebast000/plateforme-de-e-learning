import 'package:flutter/material.dart';

class ProfilePage extends StatelessWidget {
  const ProfilePage({super.key}); // Correction super.key

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Page de Profil Utilisateur (à implémenter)'));
  }
}